# Match Analysis Volley — Decimo Roma

## Contesto del progetto
Strumenti di analisi statistica per la pallavolo, sviluppati per Decimo Roma (Serie D, Roma).
Il progetto è stato migrato da Google Colab a una repo strutturata con supporto multi-ambiente.

## Struttura repo
- `notebooks/` — notebook Jupyter (classifiche.ipynb, tabellino.ipynb)
- `config/` — CSV di configurazione (seasons, matches, players, player_identities)
- `src/` — moduli Python (rankings.py, efficiency.py, attacks.py, leg_comparison.py)
- `dashboard/` — app Streamlit (`app.py`), `streamlit run dashboard/app.py`
- `data/` — esclusa dal repo, dati reali su Google Drive
- `docs/roadmap.md` — sviluppi futuri

## Ambiente
- Python 3.9 nel virtual environment `.venv`
- Attiva sempre con `source .venv/bin/activate`
- Dipendenze in `requirements.txt`
- Dati su Google Drive, accessibili in locale via:
  `/Users/Marco.Miccheli/Library/CloudStorage/GoogleDrive-marco86sim@gmail.com/Il mio Drive/Pallavolo/Decimo Roma/`
- Path configurato in `.env` tramite `VOLLEY_DATA_PATH`

## Decisioni architetturali prese
- Nel refactoring di `tabellino.ipynb` verso `src/`, tutto da "# altro" in poi (celle ad-hoc per singola partita, sezione "tabellino" standard con `create_player_summary_df`, "SO per rotazioni") resta intenzionalmente **fuori scope**, non refactorato: quelle celle restano invariate nel notebook, non spostate in `src/`
- I dati (Excel partite) restano su Google Drive, mai nel repo
- `config/paths.py` rileva automaticamente l'ambiente (Colab vs locale)
- `config/matches.csv` — una riga per partita, con colonne: season, leg, opponent, path, active
- `config/seasons.csv` — una riga per stagione
- `config/players.csv` — registro anagrafico giocatori con player_id stabile
- `config/player_identities.csv` — terne (team, cognome, numero) associate a player_id
- La cella di check terne in classifiche.ipynb usa input() interattivo — funziona sia in Colab che in Cursor (il prompt appare in cima alla finestra)
- I giocatori saltati nel check terne vengono esclusi dalle classifiche tramite filtro su player_identities.csv
- Terne da ignorare in modo permanente (es. giocatori occasionali non più di interesse) si marcano con `player_id = IGNORED` in `player_identities.csv`: il check terne le riconosce come già gestite (nessun nuovo prompt) e la cella FILTRO le esclude esplicitamente dalle classifiche
- `save_identity`/`save_player` nel notebook usano `csv.writer(f, lineterminator='\n')` per evitare terminazioni di riga miste (`\r\n`) nei CSV — da mantenere in eventuali refactor

## Stato attuale (aggiornato 2026-08-12)
- Stagione 2025-2026 completa in `config/matches.csv`: 28 partite (andata + ritorno + playout), tutte `active=1`
- `config/players.csv` / `config/player_identities.csv` popolati con 11 giocatori Decimo + 4 terne marcate `IGNORED` (Martignoni, Amore, Ferrazzi, Principato)
- `notebooks/classifiche.ipynb` verificato end-to-end senza errori (caricamento, check terne, filtro, classifiche, export PDF)
- `src/rankings.py` creato: `calculate_player_scores_by_type` + i 6 builder di classifica (Ace Man, Spike Leader, Spike Guarantee, Block Monster, Miglior Ricevitore, Muro della vergogna) estratti da `classifiche.ipynb`, che ora li importa. Le `ignore_list` per fondamentale restano nel notebook (scelta editoriale specifica della stagione, non logica riutilizzabile). Verificato: output identico pre/post refactor.
- `src/efficiency.py` creato: `calcola_efficienza`, `find_errors`, `separate_attacks_counterattacks`, `separate_free_ball`, `calcola_efficienza_free_ball`, `compute_set_metrics`, `export_tabellino_to_xlsx` estratti da `tabellino.ipynb` (celle 7, 19, che ora importano da `src.efficiency`). Il notebook aveva due copie divergenti di `find_errors`: una (usata da `export_tabellino_to_xlsx`, export "`[tabellino F]`") contava gli errori di muro, l'altra (usata dal tabellino standard, `create_player_summary_df`, sezione "altro"/"tabellino" — **fuori scope**, non toccata) no. Solo la copia usata da `export_tabellino_to_xlsx` è stata unificata per includere sempre il muro; `create_player_summary_df` resta nel notebook con la sua `find_errors` locale invariata (divergenza tra i due export ancora presente, per ora). Rimossa dead code mai chiamata nel notebook (`_attack_tipo_value`, `create_player_summary_df_with_free_ball`). Verificato eseguendo notebook baseline (pre-refactor) e post-refactor sulla stessa partita reale (JVC, con `write_files=False` per non toccare Google Drive): output identico.
- `src/attacks.py` creato: tutte le funzioni di grafico di `tabellino.ipynb` (`compute_points_table`, `plot_points_grouped`, `compute_attack_eff_breakdown`, `plot_attack_eff_breakdown_bars`, `create_attack_eff_plots`, `plot_set_efficiency_groups`, `create_metrics_plot`, `plot_set_radar`), estratte dalle celle 13/16/19/23 (tutte precedenti alla sezione "altro"). Le firme ora accettano `write_files`/`save_dir` come parametri espliciti al posto delle variabili globali del notebook (`write_files`, `file_path1`) — un modulo importato non vede i globali del notebook chiamante, quindi le celle 14/17/21/24 ora passano questi valori esplicitamente ad ogni chiamata. Verificato: eseguendo baseline pre-refactor e post-refactor sulla stessa partita reale (`write_files=False`), tutti gli output testuali e tutti i grafici PNG generati sono risultati byte-identici (hash SHA256 uguali).
- Estrazione `src/` da `tabellino.ipynb` completata per tutto ciò che precede la sezione "altro" (config, caricamento dati, tabellino con Free Ball, punti/efficienza/trend/radar). Da "altro" in poi (celle ad-hoc per singola partita, tabellino standard, "SO per rotazioni") il notebook resta byte-identico all'originale, per scelta.
- `requirements.txt` completato e pinnato alle versioni verificate funzionanti (Python 3.9): pandas 2.3.3, numpy 2.0.2, matplotlib 3.9.4, openpyxl 3.1.5, xlsxwriter 3.2.9, fpdf2 2.8.4, python-dotenv 1.2.1. Aggiunto `ipykernel` (mancava: necessario per eseguire i notebook come kernel Jupyter in Cursor/VS Code, prima installato manualmente senza essere dichiarato). Testato installando in un venv pulito (non il `.venv` del progetto) ed eseguendo end-to-end sia `classifiche.ipynb` che `tabellino.ipynb` (`write_files=False`): nessun errore.
- **Confronto andata vs ritorno** (sostituisce per ora il KPI cross-stagione, in standby finché non ci sono più stagioni popolate):
  - `src/leg_comparison.py`: carica ogni partita della stagione singolarmente (non concatenata come in classifiche.ipynb). Ogni partita è taggata con `leg` (`A`/`R`/`POA`/`POR`), `giornata` (posizione dell'avversario nel girone di andata, 0-based), `x_label` (etichetta per l'asse X del grafico 1: nome avversario per A/R, `PLAYOUT_LABEL` = "JVC (PO)" per POA/POR — il playout ha una posizione propria in coda, non è raggruppato con la partita di andata/ritorno contro JVC) e `match_seq` (indice cronologico assoluto 0..27 su tutta la stagione: andata 0-12, ritorno 13-25, POA=26, POR=27 — usato dai grafici di confronto). Filtra i giocatori riusando `config/player_identities.csv` (stessa logica della cella FILTRO di classifiche.ipynb). `build_comparison_dataset(season)` produce le tabelle tidy per la dashboard.
  - **KPI disponibili** (17 totali, `ALL_KPIS`): 4 percentuali di efficienza (Battuta%/Ricezione%/Attacco%/Muro%, via `calcola_efficienza`), 4 totali per fondamentale (`TOT_KPI_LABELS`: Battuta/Ricezione/Attacco/Muro Tot — numero di azioni di quel fondamentale a prescindere dal voto, dalla colonna `Tot` già calcolata da `calcola_efficienza`), 8 conteggi assoluti per voto specifico (`ABS_KPI_DEFS`: Attacco # punti, Muro # punti, Battuta # ace, Ricezione = ace subiti, Battuta = errori, Attacco = errori, Attacco / murati, Muro = mani-fuori subite) e il totale Errori (via `find_errors`). Sia a livello squadra (`compute_team_kpis`) che per giocatore (`compute_player_kpis`). Per i conteggi assoluti per giocatore: `NaN` (buco) se il giocatore non ha fatto **nessuna** azione di quel fondamentale in quella partita (es. libero che non attacca mai), `0` esplicito se ha giocato il fondamentale ma non ha ottenuto quello specifico voto (distinzione verificata su dati reali).
  - `dashboard/app.py` (Streamlit + Plotly), tre sezioni:
    1. **"Confronto per KPI — Squadra/Giocatore"**: per ogni entità selezionata, un grafico con barre raggruppate andata/ritorno per avversario (14ª posizione a sé stante per il playout JVC, con le due barre POA/POR affiancate) + 2 linee di trend (andata, ritorno) lisciate con un kernel gaussiano (`kernel_smooth`, bandwidth regolabile da sidebar, condiviso da tutte le sezioni) e rese con `line_shape='spline'`; i punti mancanti (giocatore non in campo quella partita) restano `NaN` e la linea si interrompe (`connectgaps=False`). Toggle "Barre e trend / Solo barre / Solo trend". L'asse X ha ordine forzato esplicitamente (`categoryorder='array', categoryarray=x_axis_order`) — **senza questo, Plotly infila le categorie nell'ordine in cui appaiono nelle singole tracce e con dati mancanti l'ordine risulta incoerente** (bug riscontrato e corretto).
    2. **"Confronto tra giocatori"**: per KPI scelto/i, una linea continua per ciascuna entità selezionata (Squadra e/o giocatori) su tutte le 28 partite (`match_seq`, playout = 27ª/28ª), colore fisso per entità (assegnato sull'elenco completo delle entità disponibili, non su quelle selezionate — funzione generica `categorical_style`). Etichette asse X: nome avversario (abbreviato per i più lunghi via `OPPONENT_ABBR`, es. "Monterotondo"→"M.tondo"), ripetuto per andata/ritorno, playout con suffisso "(PO-A)"/"(PO-R)". Tabella riepilogativa Min/Mediana/Max per entità.
    3. **"Confronto KPI per una singola entità"**: speculare alla 2 — un'entità (selectbox) e più KPI selezionati sovrapposti come linee sullo stesso grafico (`build_kpi_comparison_chart`), stesso asse X per avversario. Colore fisso per KPI (stessa `categorical_style`, ordine su `ALL_KPIS`). Se la selezione mescola KPI percentuali e conteggi assoluti, un avviso in pagina segnala che le scale non sono direttamente comparabili (nessun blocco, solo trasparenza — coerente col principio "mai doppio asse Y" della dataviz skill: qui l'asse resta unico e generico "Valore"). Tabella Min/Mediana/Max con formattazione per-riga (`format_kpi_value`) perché ogni riga può avere un'unità diversa.
  - **Dark mode**: `theme_colors()` rileva il tema Streamlit attivo via `st.context.theme.type` (`"light"`/`"dark"`, default `"light"` se `None`) e sceglie tra `PALETTE_LIGHT`/`PALETTE_DARK` (hue dataviz skill, varianti chiara/scura) e superficie/griglia/font coerenti. Ogni `st.plotly_chart(fig, theme=None)` passa esplicitamente `theme=None` — **senza, Streamlit applica il proprio tema "streamlit" sopra la figura, che può scurire lo sfondo lasciando il font hardcoded scuro invariato: testo nero su sfondo nero in dark mode** (bug riscontrato e corretto; verificato con screenshot Playwright in `color_scheme="dark"`, ora testo sempre leggibile in entrambi i temi). I tint playout (POA/POR) sono calcolati dinamicamente con `hex_to_rgba(color, 0.45)` sul blu/arancio del tema attivo, non hardcoded.
  - **Hover con valore reale**: le linee di trend sono smussate (`kernel_smooth`) solo per la resa visiva della curva — il valore mostrato in hover (`customdata` + `hovertemplate`) è sempre quello grezzo pre-smoothing, formattato per tipo di KPI (`format_kpi_value`). Prima di questo fix l'hover mostrava il valore smussato (decimale) anche per i conteggi assoluti, che invece devono sempre poter essere letti come interi esatti — verificato: hover su "Attacco # (punti)" mostra "valore reale: 40", combaciante esattamente con l'altezza della barra.
  - **KPI "Attacco SO" (side-out), "Attacco FB" (free ball) e "Contrattacco"**: 15 KPI aggiuntivi (5+5+5, stesso schema Eff%/Tot/#punti/=errori//murati) sui tre sottoinsiemi disgiunti ed esaustivi in cui `separate_attack_types` (`src/efficiency.py`) divide ogni attacco — SO = immediatamente dopo una ricezione, FB = immediatamente dopo una free ball (difesa Voto `!`, convenzione tabellino: palla facile rimandata nel nostro campo — situazione favorevole, efficienza attesa alta), Contrattacco = tutto il resto (verificato: Attacco SO Tot + Attacco FB Tot + Contrattacco Tot == Attacco Tot, sempre, per costruzione, scarto 0.0 su ogni partita della stagione 2025-2026). Prima del 2026-08-12 Contrattacco includeva anche gli attacchi dopo free ball (funzione a due vie `separate_attacks_counterattacks`) — corretto perché una free ball è concettualmente distinta da un vero contrattacco/rimessa in gioco confusa. A differenza degli altri 17 KPI (fissi per stagione), questi 15 sono **parametrici** sul voto di ricezione che qualifica l'attacco come "SO" (`rec_vote`, Attacco FB e Contrattacco non dipendono direttamente da esso ma si spostano insieme essendo complementari sullo stesso split): la dashboard espone un multiselect in sidebar ("Esito ricezione per Attacco SO", default `#,+,!,-` come il default storico della funzione) che ricalcola solo questi 15 KPI ad ogni cambio, senza toccare la cache dei 17 fissi — 32 KPI totali (`DISPLAY_KPIS` in `dashboard/app.py`). `src/leg_comparison.py`: `compute_team_attacco_so_kpis`/`compute_player_attacco_so_kpis` (calcolano le tre famiglie in una sola chiamata a `separate_attack_types`, per non farla girare più volte) + `build_attacco_so_dataset(season, rec_vote, matches=None)`; `load_all_matches` ora è cacheata separatamente (`load_matches_cached` in `dashboard/app.py`) e riusata sia dal dataset fisso che da quello parametrico, per non ricaricare gli Excel da Drive due volte.
    - **Bug corretto in `separate_attacks_counterattacks`** (`src/efficiency.py`): la funzione decideva "attacco dopo ricezione" solo per posizione (`iloc[i-1]`/`iloc[i-2]`) sul dataframe già filtrato sui giocatori riconosciuti. Un'azione di un giocatore **IGNORED** (es. Martignoni, Ferrazzi) tra la ricezione e il vero attacco successivo viene rimossa dal filtro, e la posizione "salta" agganciando erroneamente un attacco di un rally diverso alla ricezione. Verificato empiricamente su tutta la stagione 2025-2026: **5 falsi positivi su 1640** attacchi "dopo ricezione", tutti con questo identico schema. Fix: aggiunto un controllo di consistenza — ricezione (ed eventuale alzata) e attacco devono avere lo stesso `Numero Set` e lo stesso punteggio (`Punti Locali`/`Punti Ospiti`); se non coincidono, l'attacco ricade tra i contrattacchi. Ri-verificato dopo il fix: 1635 attacchi "dopo ricezione", zero violazioni residue. Lo stesso fix (stesso controllo di consistenza) è stato applicato per parità anche a `separate_free_ball`, che aveva lo stesso bug potenziale.
    - **`separate_attack_types`** (`src/efficiency.py`, nuova funzione, non sostituisce le due funzioni a due vie usate da `export_tabellino_to_xlsx`, lasciate invariate): unisce in un solo passaggio la logica di `separate_attacks_counterattacks` e `separate_free_ball` (stesso controllo di consistenza Numero Set/punteggio) per produrre i tre gruppi disgiunti SO/FB/Contrattacco usati da `leg_comparison.py`.
  - **Soglia minima attacchi** (`apply_min_attacks_threshold` in `src/leg_comparison.py`, controllo "Soglia minima attacchi" in sidebar, default 0 = nessun filtro): per i KPI della famiglia "attacco" (Attacco*, Attacco SO*, Attacco FB*, Contrattacco* — mappa `ATTACK_FAMILY_TOT_KPI`, KPI → il proprio "Tot" di famiglia), se un giocatore in una partita non raggiunge la soglia sul "Tot" corrispondente, quella riga (player, match, kpi) viene scartata da `player_df` — il giocatore risulta "senza dati" per quel KPI in quella partita (stesso trattamento del buco/NaN altrove), non un valore a zero. Si applica solo a `player_df`, non a `team_df` (la richiesta è esplicitamente "per quel giocatore"); Battuta/Ricezione/Muro/Errori non sono soggetti alla soglia. Verificato: con soglia crescente (0→20) le righe della famiglia attacco calano progressivamente mentre le righe Battuta% restano invariate; caso puntuale verificato (Cepparano, 2 Attacco SO contro Lazio, soglia=10 → riga assente).
  - **Pagella giocatore (base dati, in corso — non ancora un rendering finale)**:
    - **Presenze** (`src/attendance.py`): parsa il foglio "Presenze D" del workbook `Serie D_u19.xlsx` su Drive (cartella `.../<stagione>/Serie D/`, un livello sopra "Match analysis" — `config.paths.build_competition_root_path`). Il file è mantenuto come `.xlsx` reale (non un `.gsheet` nativo, che non sincronizza un binario locale): si edita direttamente quel file da Sheets in modalità compatibilità Excel (o da Excel/Numbers), così Drive lo sincronizza come al solito, leggibile senza export manuale ricorrente. Classifica ogni colonna evento (sigla di riga, verificata sui dati reali 2025-2026: vuoto=allenamento, `NO ALL`=allenamento non tenuto, `PESI`=escluso, `AMI`/`AMI-SG`=amichevole, `u19-4i/a-r`=evento U19, sigle con `playout`=partita di playout, `<COD>-C/T`=partita di campionato) e ogni cella stato (P/Pi/PI/P?/P_spar=presente, A=assente giustificata, I=infortunio, N=assente non giustificata, NC/`-`=non convocato, U19=altrove con l'U19, R=riposo, `?`=dato mancante, cella vuota=fuori dal periodo tracciato per quel giocatore) — categorie tutte confermate con l'utente il 2026-08-12. `filter_known_players` include solo i cognomi riconosciuti in `player_identities.csv` (non la colonna "gruppo" del foglio, che non corrisponde 1:1 a "gioca in Serie D": es. Liberatori è gruppo `2d` ma è un giocatore Decimo riconosciuto) e segnala (non scarta silenziosamente) i cognomi presenti nel foglio ma assenti dal registro (es. `Valvona` nella 2025-2026). `compute_attendance_summary`/`compute_attendance_summary_by_type`: tasso di assenza (A+I+N) sul totale dei convocati (presente+assente, esclusi NC/`-`/U19/R/fuori periodo/dato mancante dal denominatore). `list_unrecognized_statuses` segnala eventuali sigle mai viste, da controllare a inizio stagione prossima prima di fidarsi del report.
    - **Rendimento** (`src/player_report.py`): combina i KPI di `leg_comparison.py` con le presenze per produrre, per ciascun giocatore, i `top_n` (default 5) "finding" più notevoli — non tutti i 32 KPI (illeggibile) ma un sottoinsieme curato (`PAGELLA_KPIS`, richiesto esplicitamente dall'utente il 2026-08-12): Battuta%/#ace/=errori, Ricezione%/=ace subiti, Attacco scomposto in SO%/FB%/Contrattacco% (mai la versione generica non distinta) con i rispettivi punti/errori/murati, Muro # (punti) soltanto (Muro% e Muro = mani-fuori subite esclusi, "poco indicativi"), Errori totale. Due tipi di finding: **streak** (>= 3 partite giocate consecutive sopra/sotto una banda di tolleranza attorno alla mediana stagionale del giocatore: tolleranza = max(15% relativo, 0.5 deviazioni standard) — parametri concordati con l'utente) e **trend prima/seconda metà stagione** (medie a confronto, riportato solo se il delta supera la stessa tolleranza, con la pendenza di regressione lineare allegata come dato di supporto, non come finding separato). Punteggio di notabilità normalizzato sulla tolleranza propria di ciascun KPI, non sulla sua baseline: molti KPI di conteggio hanno mediana 0 (es. Muro # punti in una partita senza muri vincenti), e dividere per una baseline vicina a zero faceva esplodere lo score (bug riscontrato e corretto durante la verifica). **Esclusione per ruolo**: l'intera famiglia attacco (SO/FB/Contrattacco) è esclusa a priori per i giocatori con ruolo `P` (palleggiatore, dalla colonna "Ruolo" del foglio Presenze D) — richiesta esplicita dell'utente (Caranzetti attacca poche volte a partita, dati non significativi per il suo ruolo); una metrica specifica per palleggiatori resta da definire (vedi roadmap). `build_player_report_base` accetta un `matches` opzionale (stesso pattern degli altri builder) per riusare la cache Excel della dashboard.
    - **Validazione conteggio assenze**: confrontato il conteggio automatico con quello manuale dell'utente su 6 giocatori — 4/6 combaciavano solo aggiungendo le sessioni "pesi" (escluse di default), gli altri 2 no. L'utente ha confermato di mantenere il conteggio automatico (allenamenti + amichevoli + partite, A+I+N, pesi esclusi) come fonte di verità; lo scarto residuo sui 2 giocatori resta non spiegato (probabile imprecisione nel conteggio manuale), non ha richiesto modifiche al codice.
    - **Sezione dashboard "Pagella giocatore"** (4ª sezione di `dashboard/app.py`, dopo le 3 esistenti): una tab per giocatore Decimo riconosciuto (`st.tabs`), con ruolo/assenza%/presente-convocato in metriche, poi per ciascun finding una frase discorsiva (`format_finding_text`) e un grafico di evidenza (`build_finding_chart`): la serie grezza del KPI lungo le 28 partite con la porzione del finding evidenziata (rettangolo verde se favorevole/rosso se sfavorevole per gli streak, riga tratteggiata al punto di rottura + due segmenti orizzontali per i cambi di livello) — stessa convenzione status color della striscia vinta/persa. Usa `player_df_all` (KPI senza la soglia minima attacchi della sidebar): i finding sono calcolati sulla serie completa, i grafici devono restare coerenti. **Bug corretto**: `format_kpi_value` (pensata per un valore grezzo di singola partita, sempre intero per i conteggi) applicata alle medie (mediana stagionale, media di uno streak, media prima/seconda metà) arrotondava a "0" medie frazionarie come 0.15, producendo frasi tipo "da 0 a 0" per differenze reali ma piccole — introdotta `format_kpi_average` (2 decimali per i conteggi) per le medie, `format_kpi_value` resta solo per valori grezzi/hover.
    - **Algoritmo streak/cambio livello rivisto** (2026-08-12, feedback dell'utente su un caso reale — Pessei/Attacco SO% mostrava un aumento visibile dalla partita 20 in poi, mai segnalato):
      - **Split fisso → ricerca del punto di rottura ottimale**: `compute_half_season_trend` (split fisso a metà stagione, match_seq 14) sostituita da `detect_level_shift` in `src/player_report.py`, che cerca IL punto (tra due partite giocate consecutive, almeno `min_length` partite giocate su ciascun lato) che massimizza |media(dopo) - media(prima)|. Un cambiamento che comincia altrove (es. partita 20) veniva diluito e perso da uno split fisso a metà (delta diluito a +6.5, sotto tolleranza) — con la ricerca ottimale emerge esattamente al match_seq corretto (delta +38.5, ben sopra tolleranza). Il finding si chiama ora `'cambio_livello'` (non più `'trend_meta_stagione'`).
      - **Filtro di significatività sui KPI percentuali** (`PERCENT_KPI_VOLUME`, `DEFAULT_MIN_VOLUME_PERCENT=5`, alzata da 3 il 2026-08-13 su ulteriore richiesta esplicita dell'utente): una percentuale su pochi tentativi non è statisticamente affidabile, e gonfia anche la varianza stagionale (rendendo la tolleranza troppo larga per rilevare cambiamenti reali — esattamente il meccanismo dietro il caso Pessei). Le partite sotto soglia vengono escluse dal calcolo per quel KPI percentuale, come un buco/NaN. Motivazione dell'utente per la soglia 5: su circa 120 azioni totali a partita, un giocatore che ne ha fatte 3 di un certo tipo "è come se non avesse giocato" quel fondamentale — non abbastanza per dire come sia andato. Per i ruoli a basso volume (es. i centrali, mediana Attacco FB Tot = 2 a partita) questo esclude una parte sostanziale dei dati: conseguenza accettata consapevolmente. `compute_player_findings` ora richiede in input anche i KPI 'Tot' delle famiglie percentuali (non generano finding propri, servono solo al filtro).
      - **Streak su media mobile** (2026-08-13, richiesto esplicitamente dall'utente): `detect_streaks` classificava sopra/sotto tolleranza partita per partita — una singola partita "nella norma" in mezzo a un periodo altrimenti anomalo interrompeva lo streak, anche se il periodo nel complesso restava anomalo (perdendo crescite/cali anche solo momentanei). Ora la classificazione avviene sulla media mobile CENTRATA di ampiezza `min_length` (3), non sui valori grezzi — il valore medio riportato nella frase discorsiva resta comunque quello grezzo delle partite del periodo, non la media mobile.
      - **Riferimenti "avversario (andata/ritorno)" invece del numero di partita** (2026-08-13, richiesto esplicitamente dall'utente): `build_match_label_lookup` in `dashboard/app.py` mappa ogni `match_seq` a `"Avversario (andata/ritorno/playout andata/playout ritorno)"`; `format_finding_text` lo usa per descrivere l'inizio/fine di uno streak o il punto di rottura di un cambio di livello, con il numero di partita 1-28 riportato tra parentesi come riferimento aggiuntivo (es. "da NMC (andata) (partita 12) a JVC (playout ritorno) (partita 28)").
      - **Bug corretto (score cambio_livello)**: normalizzare il delta solo sulla tolleranza (senza moltiplicatore di ampiezza) lasciava il cambio di livello sempre sottopesato rispetto agli streak (che moltiplicano per `length`) — il caso Pessei restava fuori dalla top-5 nonostante il delta enorme. Aggiungere un moltiplicatore per `n_dopo` (partite nel nuovo livello) risolveva quel caso ma ne creava un altro: cambi di livello con delta minuscolo su un segmento enorme (es. -1.3 ace/partita su 23 partite) sovrastavano cambi con delta enorme su un segmento più corto. **Fix finale**: streak e cambio_livello sono due pool con scale di score non comparabili (`_select_balanced` in `src/player_report.py`) — ciascuno ordinato per il proprio score, poi combinati riservando metà dei `top_n` posti a ciascun tipo (l'altro tipo riempie gli slot residui se uno dei due pool ne ha meno).
    - **Bug corretto (`add_result_strip`, preesistente, non introdotto oggi)**: il confronto `esito is True`/`esito is False` non funziona mai per un valore booleano estratto da una colonna pandas (`numpy.bool_`, non `bool` nativo — `numpy.True_ is True` vale `False` in Python): la striscia risultava **sempre grigia**, qualunque fosse il vero esito. Mai notato prima (la differenza grigio/colorato passa facilmente inosservata a uno sguardo superficiale su uno screenshot). Fix: `esito is None` (per la chiave assente, corretto — `Series.get()` ritorna `None` di default) poi truthiness (`elif esito:`) invece di `is True`/`is False`. Riverificato con i dati grezzi della figura (non solo a occhio): colori e testo hover combaciano esattamente con `partita_vinta` per tutte le 28 partite.
    - **File risultati ufficiali estesi ai playout**: l'utente ha aggiunto a mano al file `risultati_decimo_{season}.txt` due blocchi `Playout Andata`/`Playout Ritorno` (il sito federale non ha un export automatico per i playout) — `parse_official_results` ora li riconosce (mappati su giornata 27/28, stesso schema `match_seq + 1`) oltre ai blocchi `Giornata N`. Un punteggio aveva un prefisso non numerico (`#11/15`, playout ritorno — probabile nota procedurale della federazione): `_parse_set_score` estrae solo le cifre invece di fallire su `int('#11')`. Risultato: **28/28 partite esatte** (`esatto=True` su tutte), 0 indeterminate. Cross-validato: 0 mismatch anche sui 2 playout tra `decimo_locali` e la squadra Decimo dedotta dal file ufficiale.
    - **Report di sintesi stagionale per giocatore** (`src/player_season_report.py`, richiesto esplicitamente dall'utente il 2026-08-14): a differenza dei "finding" di `player_report.py` (periodi puntuali nel corso della stagione), qui è una vista d'insieme sul rendimento stagionale, curata **per ruolo** (colonna "Ruolo" del foglio Presenze D — L/M/C/O, gli ibridi "M/L"/"M/O"/"M/O/L" ricadono sul profilo martello, il più inclusivo; palleggiatori rimandati, "discorso a parte" non ancora definito):
      - **Libero**: solo Ricezione% (efficienza aggregata di stagione). **Martello**: Ricezione%, Attacco% generale + SO/FB/Contrattacco separati, Muro # e Muro = (mani-fuori subite), Battuta scomposta in = errori/# ace/{+,!,/} mette in difficoltà/- conservativa. **Centrale**: Muro # e Muro + (mani-out che permettono il contrattacco) in primo piano, Attacco% (con enfasi esplicita "tanti attacchi ma efficienza bassa = problema"). **Opposto**: Attacco # (punti) in primo piano (tollerato un tasso di errore più alto), Muro rilevante.
      - **Voti grezzi non coperti da `ABS_KPI_DEFS`**: `_build_raw_voto_dataset` estrae conteggi per (tipo, voto) non già esposti da `calcola_efficienza`/`ABS_KPI_DEFS` in `leg_comparison.py` — Muro `/` (l'errore di muro, convenzione `find_errors`: distinto da "Muro = mani-fuori subite"), Muro `+`, Attacco `+`/`-`, Battuta `+`/`!`/`/` sommati (mette in difficoltà) e Battuta `-` (conservativa). Non aggiunti ad `ABS_KPI_DEFS`/`ALL_KPIS` (restano fuori dai 32 KPI della dashboard esistente): calcolati solo per questo report.
      - **Punteggio composito di partita** ("efficienza totale, apporto positivo alla squadra", richiesto esplicitamente): punti fatti (Battuta # ace + Muro # punti + Attacco # punti) meno errori fatti (Attacco = errori + Battuta = errori + Muro `/` errore — non Muro `=`, su indicazione esplicita dell'utente). `compute_composite_scores`: netto=0 (non un buco) per un giocatore che ha giocato la partita ma non ha prodotto nessuna di queste azioni.
      - **Partite notevoli**: `find_notable_matches`, per i 6 KPI del punteggio composito, la singola partita più alta/bassa **nel verso giusto secondo `KPI_DIRECTION`** (per gli errori: la partita con MENO errori è "migliore"), riportata solo se lo scarto dalla mediana stagionale supera la stessa tolleranza di `player_report.py`. `find_best_worst_composite_match`: idem per il punteggio composito — ritorna `(None, None)` se il "netto" non varia mai (es. un libero, che non contribuisce al composito: `idxmax`/`idxmin` su valori tutti a 0 darebbero un falso migliore/peggiore coincidenti sulla prima riga).
      - **Punti di forza/debolezza**: soglie assolute concordate esplicitamente con l'utente (confermate "valori che mi aspetto da un buon campionato") — Attacco SO% aggregato < 20% e Contrattacco% aggregato < 0% → da migliorare; media errori battuta/partita > 2 → da migliorare. Più confronto con la **media** dei compagni di ruolo (banda di tolleranza 15%, coerente col resto del progetto) su Muro #/Muro + /Attacco% per i centrali, Attacco # (punti) per Martelli+Opposto insieme (nella rosa 2025-2026 c'è un solo opposto: il confronto solo-ruolo sarebbe vacuo, richiesta esplicita dell'utente di pooling).
      - **Obiettivi per la prossima stagione** (richiesto esplicitamente il 2026-08-14, "tenersi sopra la mediana del kpi in esame"): `_compute_median_targets`, per ogni KPI del proprio ruolo, confronto con la **mediana** (non la media, e senza banda di tolleranza — un obiettivo, non un giudizio di problema) del gruppo di riferimento (`_median_peer_buckets`: Libero+Martello per la ricezione, Martello+Opposto per attacco/muro salvo i Centrali che restano un gruppo a sé, Martello da solo per la battuta — sempre per evitare mediane calcolabili su un solo giocatore). **Bug corretto in verifica**: la prima versione confrontava sempre "sotto la mediana = da migliorare", sbagliato per i KPI dove basso è un bene (es. errori) — un giocatore con MENO errori della mediana veniva segnalato per errore come "da migliorare". Fix: la direzione (`KPI_DIRECTION`/`EXTRA_KPI_DIRECTION` unificate in `_count_kpi_direction`) decide il verso giusto; i KPI a direzione neutra (es. Battuta - conservativa, uno stile non una qualità) sono esclusi.
      - Sezione dashboard "Sintesi stagionale" dentro ogni tab giocatore della "Pagella giocatore" (prima dei finding puntuali già esistenti): efficienze aggregate, conteggi medi/partita, punti di forza/debolezza, obiettivi, partita migliore/peggiore (composito), partite singole notevoli — usa lo stesso `build_match_label_lookup` per i riferimenti avversario/leg.
  - Palette CVD-safe da dataviz skill (blu=andata, arancio=ritorno, stesse hue anche per le linee di trend; playout = stessa hue con opacità ridotta; 8 hue categoriche fisse per entità/KPI nei grafici 2-3, con tratteggio a rotazione oltre l'ottava).
  - Verificato su più livelli: (1) i KPI di `compute_team_kpis`/`compute_player_kpis` (incluse le metriche assolute e i totali per fondamentale) coincidono esattamente con chiamate dirette a `calcola_efficienza`/`find_errors`; (2) `kernel_smooth` preserva i NaN nelle posizioni originariamente mancanti (verificato numericamente su Pessei 13/Attacco%, buco a "Monterotondo"); (3) app testata headless con `streamlit.testing.v1.AppTest` in un venv pulito con solo `requirements.txt` — nessuna eccezione su selezione multipla completa (tutti e 17 i KPI) su tutte e 3 le sezioni; (4) screenshot reali (Playwright, solo per verifica, non è una dipendenza del progetto) confermano visivamente l'ordine corretto dell'asse X, i buchi nelle linee di trend, la sovrapposizione multi-KPI, la leggibilità in dark mode e l'hover con valore grezzo.
  - Avvio: `streamlit run dashboard/app.py`. Nuove dipendenze in `requirements.txt`: `streamlit==1.50.0`, `plotly==6.9.0`.
  - **Nota per `st.plotly_chart`**: in Streamlit 1.50 non esiste ancora un parametro `width` per questo elemento (usa `use_container_width`, già `True` di default) — passare `width="stretch"` (valido invece per `st.dataframe`) lo instrada nei `**kwargs` legacy e genera un warning visibile in pagina. Non aggiungere `width=` a `st.plotly_chart` finché la libreria non lo supporta esplicitamente.
  - **Esiti partite/set (in preparazione a un futuro modello random forest su cosa determina vittorie/set/differenza punti)**:
    - **Chi è "Locali"/"Ospiti" nell'Excel**: la prima riga di ogni file Excel (saltata da `skiprows=1` nel caricamento dati normale) contiene una dicitura autoritativa `Locali: <squadra>` / `Ospiti: <squadra>` — l'unica fonte non ambigua per sapere se Decimo era la squadra "Locali" o "Ospiti" in quella partita (l'euristica precedente basata sul nome della cartella aveva 5 mismatch su 28). `_parse_decimo_locali(file_path)` in `src/leg_comparison.py` legge questa riga con `pd.read_excel(header=None, nrows=1)`, e solleva `ValueError` se non trova la dicitura o se non menziona "Decimo" (nessuna euristica silenziosa). `load_all_matches` allega il risultato come `decimo_locali` ("a"/"b") a ogni partita.
    - **Il punteggio esatto di fine set non è ricavabile dal solo log Excel**: ogni riga del log mostra il punteggio *prima* dell'esito di quella stessa riga, e l'incremento che porta al punteggio vincente non è mai visibile a riga (il set successivo riparte da 0-0) — ricostruire il massimo per set dà quindi sempre "24", mai "25". Sono inoltre possibili salti di punteggio >1 tra righe visibili (rally non toccati da Decimo, invisibili al log). Per questo la ricostruzione dal solo Excel resta solo una **stima approssimata**, mai la fonte primaria quando disponibile un'alternativa esatta.
    - **File risultati ufficiali** (fonte primaria): `risultati_decimo_{season}.txt` sul Drive, nella cartella "Match analysis" della stagione (es. `.../2025-2026/Serie D/Match analysis/risultati_decimo_2025-2026.txt`) — testo copiato dal sito della federazione, blocchi "Giornata N" con righe tab-separated (`N. / Data / Località / Squadra A / Squadra B / RIS / PARZIALI`). `parse_official_results(file_path)` lo legge in un dict `{giornata: {squadra_a, squadra_b, sets_a, sets_b, parziali}}` (`giornata` = posizione nel file, 1-indexed, corrisponde esattamente a `match_seq + 1` per le partite di campionato). Copre solo la regular season (26 giornate per la 2025-2026); i 2 playout non hanno un file equivalente per ora.
    - `compute_set_outcomes(df_match, decimo_locali, official_result=None)` / `compute_match_outcome(df_match, decimo_locali, official_result=None)`: se `official_result` è disponibile (regular season), delegano a una variante `_from_official` che calcola gli esiti **esatti** dai parziali del file federale; altrimenti (playout POA/POR) ricadono sulla ricostruzione approssimata dal log Excel, con `set_completo=False`/`set_vinto=None` quando il punteggio non è determinabile (es. set troncati). Cross-validato: 0 mismatch su 26 partite tra `decimo_locali` (riga 0 Excel) e la squadra Decimo dedotta dal file ufficiale — le due fonti indipendenti si confermano a vicenda.
    - `build_match_outcomes_dataset(season, matches=None)` / `build_set_outcomes_dataset(...)` in `src/leg_comparison.py`: tabelle tidy per partita/per set con `partita_vinta`/`set_vinto`, `punti_diff_totale`, `n_set_vinti`/`persi`, e un flag `esatto` (`True` se dal file ufficiale, `False` se dalla stima approssimata) — non si nasconde mai quale delle due fonti ha prodotto una riga. Il file risultati ufficiali copre anche i 2 playout (blocchi `Playout Andata`/`Playout Ritorno`, aggiunti a mano dall'utente il 2026-08-12 — non hanno un export federale automatico come il campionato regolare): `parse_official_results` li mappa su giornata 27/28 (stesso schema `match_seq + 1`). Un punteggio del file aveva un prefisso `#` (`#11/15`, playout ritorno) — `_parse_set_score` estrae solo le cifre, tollerando note procedurali non numeriche senza fallire. Risultato stagione 2025-2026: **28/28 partite esatte** (`esatto=True` su tutte), 11 vittorie regular season + playout andata vinto/ritorno perso; 0 partite indeterminate (0 righe grigie nella striscia vinta/persa in dashboard). Cross-validato: 0 mismatch anche sui 2 playout tra `decimo_locali` (riga 0 Excel) e la squadra Decimo dedotta dal file ufficiale.
    - **Striscia vittoria/sconfitta in dashboard**: nelle sezioni 2 ("Confronto tra giocatori") e 3 ("Confronto KPI per una singola entità") ogni grafico è ora un `make_subplots(rows=2, shared_xaxes=True)` con una seconda riga sottile (`add_result_strip` in `dashboard/app.py`) che mostra un quadratino verde (vittoria), rosso (sconfitta) o grigio (esito indeterminato) per ciascuna delle 28 partite, allineato esattamente alle stesse posizioni X del grafico principale — colori fissi non tematizzati (`WIN_COLOR`/`LOSS_COLOR`/`UNKNOWN_COLOR`, coerente col principio "status color" della dataviz skill). La sezione 1 (per singolo avversario, barre andata/ritorno) non ha la striscia, perché lì l'asse X non è temporale. Verificato con screenshot Playwright: quadratini allineati 1:1 sotto le etichette avversario, i 2 playout correttamente grigi (nessun file ufficiale per loro).

## Workflow settimanale (nuova partita)
1. Aggiungi riga in `config/matches.csv`
2. Apri `notebooks/classifiche.ipynb` in Cursor o Colab
3. Esegui cella per cella — il check terne rileva nuovi giocatori
4. Committa `config/players.csv` e `config/player_identities.csv` dopo il check
5. Push su GitHub

## Prossimi sviluppi (vedi docs/roadmap.md)
- Confronto KPI cross-stagione (in standby, richiede più stagioni)
- Eventuale estrazione della sezione "altro" di tabellino.ipynb
- Aggiunta stagione 2026-2027 a settembre
