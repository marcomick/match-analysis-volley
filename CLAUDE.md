# Match Analysis Volley — Decimo Roma

## Contesto del progetto
Strumenti di analisi statistica per la pallavolo, sviluppati per Decimo Roma (Serie D, Roma).
Il progetto è stato migrato da Google Colab a una repo strutturata con supporto multi-ambiente.

## Struttura repo
- `notebooks/` — notebook Jupyter (classifiche.ipynb, tabellino.ipynb)
- `config/` — CSV di configurazione (seasons, matches, players, player_identities)
- `src/` — moduli Python (rankings.py, efficiency.py, attacks.py, leg_comparison.py)
- `dashboard/` — app Streamlit (`app.py`), `streamlit run dashboard/app.py`
- `data/` — esclusa dal repo, dati reali su Google Drive
- `docs/roadmap.md` — sviluppi futuri

## Ambiente
- Python 3.9 nel virtual environment `.venv`
- Attiva sempre con `source .venv/bin/activate`
- Dipendenze in `requirements.txt`
- Dati su Google Drive, accessibili in locale via:
  `/Users/Marco.Miccheli/Library/CloudStorage/GoogleDrive-marco86sim@gmail.com/Il mio Drive/Pallavolo/Decimo Roma/`
- Path configurato in `.env` tramite `VOLLEY_DATA_PATH`

## Decisioni architetturali prese
- Nel refactoring di `tabellino.ipynb` verso `src/`, tutto da "# altro" in poi (celle ad-hoc per singola partita, sezione "tabellino" standard con `create_player_summary_df`, "SO per rotazioni") resta intenzionalmente **fuori scope**, non refactorato: quelle celle restano invariate nel notebook, non spostate in `src/`
- I dati (Excel partite) restano su Google Drive, mai nel repo
- `config/paths.py` rileva automaticamente l'ambiente (Colab vs locale)
- `config/matches.csv` — una riga per partita, con colonne: season, leg, opponent, path, active
- `config/seasons.csv` — una riga per stagione
- `config/players.csv` — registro anagrafico giocatori con player_id stabile
- `config/player_identities.csv` — terne (team, cognome, numero) associate a player_id
- La cella di check terne in classifiche.ipynb usa input() interattivo — funziona sia in Colab che in Cursor (il prompt appare in cima alla finestra)
- I giocatori saltati nel check terne vengono esclusi dalle classifiche tramite filtro su player_identities.csv
- Terne da ignorare in modo permanente (es. giocatori occasionali non più di interesse) si marcano con `player_id = IGNORED` in `player_identities.csv`: il check terne le riconosce come già gestite (nessun nuovo prompt) e la cella FILTRO le esclude esplicitamente dalle classifiche
- `save_identity`/`save_player` nel notebook usano `csv.writer(f, lineterminator='\n')` per evitare terminazioni di riga miste (`\r\n`) nei CSV — da mantenere in eventuali refactor

## Stato attuale (aggiornato 2026-08-12)
- Stagione 2025-2026 completa in `config/matches.csv`: 28 partite (andata + ritorno + playoff), tutte `active=1`
- `config/players.csv` / `config/player_identities.csv` popolati con 11 giocatori Decimo + 4 terne marcate `IGNORED` (Martignoni, Amore, Ferrazzi, Principato)
- `notebooks/classifiche.ipynb` verificato end-to-end senza errori (caricamento, check terne, filtro, classifiche, export PDF)
- `src/rankings.py` creato: `calculate_player_scores_by_type` + i 6 builder di classifica (Ace Man, Spike Leader, Spike Guarantee, Block Monster, Miglior Ricevitore, Muro della vergogna) estratti da `classifiche.ipynb`, che ora li importa. Le `ignore_list` per fondamentale restano nel notebook (scelta editoriale specifica della stagione, non logica riutilizzabile). Verificato: output identico pre/post refactor.
- `src/efficiency.py` creato: `calcola_efficienza`, `find_errors`, `separate_attacks_counterattacks`, `separate_free_ball`, `calcola_efficienza_free_ball`, `compute_set_metrics`, `export_tabellino_to_xlsx` estratti da `tabellino.ipynb` (celle 7, 19, che ora importano da `src.efficiency`). Il notebook aveva due copie divergenti di `find_errors`: una (usata da `export_tabellino_to_xlsx`, export "`[tabellino F]`") contava gli errori di muro, l'altra (usata dal tabellino standard, `create_player_summary_df`, sezione "altro"/"tabellino" — **fuori scope**, non toccata) no. Solo la copia usata da `export_tabellino_to_xlsx` è stata unificata per includere sempre il muro; `create_player_summary_df` resta nel notebook con la sua `find_errors` locale invariata (divergenza tra i due export ancora presente, per ora). Rimossa dead code mai chiamata nel notebook (`_attack_tipo_value`, `create_player_summary_df_with_free_ball`). Verificato eseguendo notebook baseline (pre-refactor) e post-refactor sulla stessa partita reale (JVC, con `write_files=False` per non toccare Google Drive): output identico.
- `src/attacks.py` creato: tutte le funzioni di grafico di `tabellino.ipynb` (`compute_points_table`, `plot_points_grouped`, `compute_attack_eff_breakdown`, `plot_attack_eff_breakdown_bars`, `create_attack_eff_plots`, `plot_set_efficiency_groups`, `create_metrics_plot`, `plot_set_radar`), estratte dalle celle 13/16/19/23 (tutte precedenti alla sezione "altro"). Le firme ora accettano `write_files`/`save_dir` come parametri espliciti al posto delle variabili globali del notebook (`write_files`, `file_path1`) — un modulo importato non vede i globali del notebook chiamante, quindi le celle 14/17/21/24 ora passano questi valori esplicitamente ad ogni chiamata. Verificato: eseguendo baseline pre-refactor e post-refactor sulla stessa partita reale (`write_files=False`), tutti gli output testuali e tutti i grafici PNG generati sono risultati byte-identici (hash SHA256 uguali).
- Estrazione `src/` da `tabellino.ipynb` completata per tutto ciò che precede la sezione "altro" (config, caricamento dati, tabellino con Free Ball, punti/efficienza/trend/radar). Da "altro" in poi (celle ad-hoc per singola partita, tabellino standard, "SO per rotazioni") il notebook resta byte-identico all'originale, per scelta.
- `requirements.txt` completato e pinnato alle versioni verificate funzionanti (Python 3.9): pandas 2.3.3, numpy 2.0.2, matplotlib 3.9.4, openpyxl 3.1.5, xlsxwriter 3.2.9, fpdf2 2.8.4, python-dotenv 1.2.1. Aggiunto `ipykernel` (mancava: necessario per eseguire i notebook come kernel Jupyter in Cursor/VS Code, prima installato manualmente senza essere dichiarato). Testato installando in un venv pulito (non il `.venv` del progetto) ed eseguendo end-to-end sia `classifiche.ipynb` che `tabellino.ipynb` (`write_files=False`): nessun errore.
- **Confronto andata vs ritorno** (sostituisce per ora il KPI cross-stagione, in standby finché non ci sono più stagioni popolate):
  - `src/leg_comparison.py`: carica ogni partita della stagione singolarmente (non concatenata come in classifiche.ipynb). Ogni partita è taggata con `leg` (`A`/`R`/`POA`/`POR`), `giornata` (posizione dell'avversario nel girone di andata, 0-based), `x_label` (etichetta per l'asse X del grafico 1: nome avversario per A/R, `PLAYOFF_LABEL` = "JVC (PO)" per POA/POR — il playoff ha una posizione propria in coda, non è raggruppato con la partita di andata/ritorno contro JVC) e `match_seq` (indice cronologico assoluto 0..27 su tutta la stagione: andata 0-12, ritorno 13-25, POA=26, POR=27 — usato dai grafici di confronto). Filtra i giocatori riusando `config/player_identities.csv` (stessa logica della cella FILTRO di classifiche.ipynb). `build_comparison_dataset(season)` produce le tabelle tidy per la dashboard.
  - **KPI disponibili** (17 totali, `ALL_KPIS`): 4 percentuali di efficienza (Battuta%/Ricezione%/Attacco%/Muro%, via `calcola_efficienza`), 4 totali per fondamentale (`TOT_KPI_LABELS`: Battuta/Ricezione/Attacco/Muro Tot — numero di azioni di quel fondamentale a prescindere dal voto, dalla colonna `Tot` già calcolata da `calcola_efficienza`), 8 conteggi assoluti per voto specifico (`ABS_KPI_DEFS`: Attacco # punti, Muro # punti, Battuta # ace, Ricezione = ace subiti, Battuta = errori, Attacco = errori, Attacco / murati, Muro = mani-fuori subite) e il totale Errori (via `find_errors`). Sia a livello squadra (`compute_team_kpis`) che per giocatore (`compute_player_kpis`). Per i conteggi assoluti per giocatore: `NaN` (buco) se il giocatore non ha fatto **nessuna** azione di quel fondamentale in quella partita (es. libero che non attacca mai), `0` esplicito se ha giocato il fondamentale ma non ha ottenuto quello specifico voto (distinzione verificata su dati reali).
  - `dashboard/app.py` (Streamlit + Plotly), tre sezioni:
    1. **"Confronto per KPI — Squadra/Giocatore"**: per ogni entità selezionata, un grafico con barre raggruppate andata/ritorno per avversario (14ª posizione a sé stante per il playoff JVC, con le due barre POA/POR affiancate) + 2 linee di trend (andata, ritorno) lisciate con un kernel gaussiano (`kernel_smooth`, bandwidth regolabile da sidebar, condiviso da tutte le sezioni) e rese con `line_shape='spline'`; i punti mancanti (giocatore non in campo quella partita) restano `NaN` e la linea si interrompe (`connectgaps=False`). Toggle "Barre e trend / Solo barre / Solo trend". L'asse X ha ordine forzato esplicitamente (`categoryorder='array', categoryarray=x_axis_order`) — **senza questo, Plotly infila le categorie nell'ordine in cui appaiono nelle singole tracce e con dati mancanti l'ordine risulta incoerente** (bug riscontrato e corretto).
    2. **"Confronto tra giocatori"**: per KPI scelto/i, una linea continua per ciascuna entità selezionata (Squadra e/o giocatori) su tutte le 28 partite (`match_seq`, playoff = 27ª/28ª), colore fisso per entità (assegnato sull'elenco completo delle entità disponibili, non su quelle selezionate — funzione generica `categorical_style`). Etichette asse X: nome avversario (abbreviato per i più lunghi via `OPPONENT_ABBR`, es. "Monterotondo"→"M.tondo"), ripetuto per andata/ritorno, playoff con suffisso "(PO-A)"/"(PO-R)". Tabella riepilogativa Min/Mediana/Max per entità.
    3. **"Confronto KPI per una singola entità"**: speculare alla 2 — un'entità (selectbox) e più KPI selezionati sovrapposti come linee sullo stesso grafico (`build_kpi_comparison_chart`), stesso asse X per avversario. Colore fisso per KPI (stessa `categorical_style`, ordine su `ALL_KPIS`). Se la selezione mescola KPI percentuali e conteggi assoluti, un avviso in pagina segnala che le scale non sono direttamente comparabili (nessun blocco, solo trasparenza — coerente col principio "mai doppio asse Y" della dataviz skill: qui l'asse resta unico e generico "Valore"). Tabella Min/Mediana/Max con formattazione per-riga (`format_kpi_value`) perché ogni riga può avere un'unità diversa.
  - **Dark mode**: `theme_colors()` rileva il tema Streamlit attivo via `st.context.theme.type` (`"light"`/`"dark"`, default `"light"` se `None`) e sceglie tra `PALETTE_LIGHT`/`PALETTE_DARK` (hue dataviz skill, varianti chiara/scura) e superficie/griglia/font coerenti. Ogni `st.plotly_chart(fig, theme=None)` passa esplicitamente `theme=None` — **senza, Streamlit applica il proprio tema "streamlit" sopra la figura, che può scurire lo sfondo lasciando il font hardcoded scuro invariato: testo nero su sfondo nero in dark mode** (bug riscontrato e corretto; verificato con screenshot Playwright in `color_scheme="dark"`, ora testo sempre leggibile in entrambi i temi). I tint playoff (POA/POR) sono calcolati dinamicamente con `hex_to_rgba(color, 0.45)` sul blu/arancio del tema attivo, non hardcoded.
  - **Hover con valore reale**: le linee di trend sono smussate (`kernel_smooth`) solo per la resa visiva della curva — il valore mostrato in hover (`customdata` + `hovertemplate`) è sempre quello grezzo pre-smoothing, formattato per tipo di KPI (`format_kpi_value`). Prima di questo fix l'hover mostrava il valore smussato (decimale) anche per i conteggi assoluti, che invece devono sempre poter essere letti come interi esatti — verificato: hover su "Attacco # (punti)" mostra "valore reale: 40", combaciante esattamente con l'altezza della barra.
  - **KPI "Attacco SO" (side-out) e "Contrattacco"**: 10 KPI aggiuntivi (5 + 5, stesso schema Eff%/Tot/#punti/=errori//murati) sui due sottoinsiemi esaustivi in cui `separate_attacks_counterattacks` (`src/efficiency.py`) divide ogni attacco — SO = immediatamente dopo una ricezione, Contrattacco = tutto il resto (verificato: Attacco SO Tot + Contrattacco Tot == Attacco Tot, sempre, per costruzione). A differenza degli altri 17 KPI (fissi per stagione), questi 10 sono **parametrici** sul voto di ricezione che qualifica l'attacco come "SO" (`rec_vote`): la dashboard espone un multiselect in sidebar ("Esito ricezione per Attacco SO", default `#,+,!,-` come il default storico della funzione) che ricalcola solo questi 10 KPI ad ogni cambio, senza toccare la cache dei 17 fissi — 27 KPI totali (`DISPLAY_KPIS` in `dashboard/app.py`). `src/leg_comparison.py`: `compute_team_attacco_so_kpis`/`compute_player_attacco_so_kpis` (calcolano entrambe le famiglie in una sola chiamata a `separate_attacks_counterattacks`, per non farla girare due volte) + `build_attacco_so_dataset(season, rec_vote, matches=None)`; `load_all_matches` ora è cacheata separatamente (`load_matches_cached` in `dashboard/app.py`) e riusata sia dal dataset fisso che da quello parametrico, per non ricaricare gli Excel da Drive due volte.
    - **Bug corretto in `separate_attacks_counterattacks`** (`src/efficiency.py`): la funzione decideva "attacco dopo ricezione" solo per posizione (`iloc[i-1]`/`iloc[i-2]`) sul dataframe già filtrato sui giocatori riconosciuti. Un'azione di un giocatore **IGNORED** (es. Martignoni, Ferrazzi) tra la ricezione e il vero attacco successivo viene rimossa dal filtro, e la posizione "salta" agganciando erroneamente un attacco di un rally diverso alla ricezione. Verificato empiricamente su tutta la stagione 2025-2026: **5 falsi positivi su 1640** attacchi "dopo ricezione", tutti con questo identico schema. Fix: aggiunto un controllo di consistenza — ricezione (ed eventuale alzata) e attacco devono avere lo stesso `Numero Set` e lo stesso punteggio (`Punti Locali`/`Punti Ospiti`); se non coincidono, l'attacco ricade tra i contrattacchi. Ri-verificato dopo il fix: 1635 attacchi "dopo ricezione", zero violazioni residue.
  - **Soglia minima attacchi** (`apply_min_attacks_threshold` in `src/leg_comparison.py`, controllo "Soglia minima attacchi" in sidebar, default 0 = nessun filtro): per i KPI della famiglia "attacco" (Attacco*, Attacco SO*, Contrattacco* — mappa `ATTACK_FAMILY_TOT_KPI`, KPI → il proprio "Tot" di famiglia), se un giocatore in una partita non raggiunge la soglia sul "Tot" corrispondente, quella riga (player, match, kpi) viene scartata da `player_df` — il giocatore risulta "senza dati" per quel KPI in quella partita (stesso trattamento del buco/NaN altrove), non un valore a zero. Si applica solo a `player_df`, non a `team_df` (la richiesta è esplicitamente "per quel giocatore"); Battuta/Ricezione/Muro/Errori non sono soggetti alla soglia. Verificato: con soglia crescente (0→20) le righe della famiglia attacco calano progressivamente mentre le righe Battuta% restano invariate; caso puntuale verificato (Cepparano, 2 Attacco SO contro Lazio, soglia=10 → riga assente).
  - Palette CVD-safe da dataviz skill (blu=andata, arancio=ritorno, stesse hue anche per le linee di trend; playoff = stessa hue con opacità ridotta; 8 hue categoriche fisse per entità/KPI nei grafici 2-3, con tratteggio a rotazione oltre l'ottava).
  - Verificato su più livelli: (1) i KPI di `compute_team_kpis`/`compute_player_kpis` (incluse le metriche assolute e i totali per fondamentale) coincidono esattamente con chiamate dirette a `calcola_efficienza`/`find_errors`; (2) `kernel_smooth` preserva i NaN nelle posizioni originariamente mancanti (verificato numericamente su Pessei 13/Attacco%, buco a "Monterotondo"); (3) app testata headless con `streamlit.testing.v1.AppTest` in un venv pulito con solo `requirements.txt` — nessuna eccezione su selezione multipla completa (tutti e 17 i KPI) su tutte e 3 le sezioni; (4) screenshot reali (Playwright, solo per verifica, non è una dipendenza del progetto) confermano visivamente l'ordine corretto dell'asse X, i buchi nelle linee di trend, la sovrapposizione multi-KPI, la leggibilità in dark mode e l'hover con valore grezzo.
  - Avvio: `streamlit run dashboard/app.py`. Nuove dipendenze in `requirements.txt`: `streamlit==1.50.0`, `plotly==6.9.0`.
  - **Nota per `st.plotly_chart`**: in Streamlit 1.50 non esiste ancora un parametro `width` per questo elemento (usa `use_container_width`, già `True` di default) — passare `width="stretch"` (valido invece per `st.dataframe`) lo instrada nei `**kwargs` legacy e genera un warning visibile in pagina. Non aggiungere `width=` a `st.plotly_chart` finché la libreria non lo supporta esplicitamente.
  - **Esiti partite/set (in preparazione a un futuro modello random forest su cosa determina vittorie/set/differenza punti)**:
    - **Chi è "Locali"/"Ospiti" nell'Excel**: la prima riga di ogni file Excel (saltata da `skiprows=1` nel caricamento dati normale) contiene una dicitura autoritativa `Locali: <squadra>` / `Ospiti: <squadra>` — l'unica fonte non ambigua per sapere se Decimo era la squadra "Locali" o "Ospiti" in quella partita (l'euristica precedente basata sul nome della cartella aveva 5 mismatch su 28). `_parse_decimo_locali(file_path)` in `src/leg_comparison.py` legge questa riga con `pd.read_excel(header=None, nrows=1)`, e solleva `ValueError` se non trova la dicitura o se non menziona "Decimo" (nessuna euristica silenziosa). `load_all_matches` allega il risultato come `decimo_locali` ("a"/"b") a ogni partita.
    - **Il punteggio esatto di fine set non è ricavabile dal solo log Excel**: ogni riga del log mostra il punteggio *prima* dell'esito di quella stessa riga, e l'incremento che porta al punteggio vincente non è mai visibile a riga (il set successivo riparte da 0-0) — ricostruire il massimo per set dà quindi sempre "24", mai "25". Sono inoltre possibili salti di punteggio >1 tra righe visibili (rally non toccati da Decimo, invisibili al log). Per questo la ricostruzione dal solo Excel resta solo una **stima approssimata**, mai la fonte primaria quando disponibile un'alternativa esatta.
    - **File risultati ufficiali** (fonte primaria): `risultati_decimo_{season}.txt` sul Drive, nella cartella "Match analysis" della stagione (es. `.../2025-2026/Serie D/Match analysis/risultati_decimo_2025-2026.txt`) — testo copiato dal sito della federazione, blocchi "Giornata N" con righe tab-separated (`N. / Data / Località / Squadra A / Squadra B / RIS / PARZIALI`). `parse_official_results(file_path)` lo legge in un dict `{giornata: {squadra_a, squadra_b, sets_a, sets_b, parziali}}` (`giornata` = posizione nel file, 1-indexed, corrisponde esattamente a `match_seq + 1` per le partite di campionato). Copre solo la regular season (26 giornate per la 2025-2026); i 2 playoff non hanno un file equivalente per ora.
    - `compute_set_outcomes(df_match, decimo_locali, official_result=None)` / `compute_match_outcome(df_match, decimo_locali, official_result=None)`: se `official_result` è disponibile (regular season), delegano a una variante `_from_official` che calcola gli esiti **esatti** dai parziali del file federale; altrimenti (playoff POA/POR) ricadono sulla ricostruzione approssimata dal log Excel, con `set_completo=False`/`set_vinto=None` quando il punteggio non è determinabile (es. set troncati). Cross-validato: 0 mismatch su 26 partite tra `decimo_locali` (riga 0 Excel) e la squadra Decimo dedotta dal file ufficiale — le due fonti indipendenti si confermano a vicenda.
    - `build_match_outcomes_dataset(season, matches=None)` / `build_set_outcomes_dataset(...)` in `src/leg_comparison.py`: tabelle tidy per partita/per set con `partita_vinta`/`set_vinto`, `punti_diff_totale`, `n_set_vinti`/`persi`, e un flag `esatto` (`True` se dal file ufficiale, `False` se dalla stima approssimata) — non si nasconde mai quale delle due fonti ha prodotto una riga. Risultato stagione 2025-2026 (26 partite esatte): 11 vittorie, 15 sconfitte; i 2 playoff restano indeterminati/approssimati.
    - **Striscia vittoria/sconfitta in dashboard**: nelle sezioni 2 ("Confronto tra giocatori") e 3 ("Confronto KPI per una singola entità") ogni grafico è ora un `make_subplots(rows=2, shared_xaxes=True)` con una seconda riga sottile (`add_result_strip` in `dashboard/app.py`) che mostra un quadratino verde (vittoria), rosso (sconfitta) o grigio (esito indeterminato) per ciascuna delle 28 partite, allineato esattamente alle stesse posizioni X del grafico principale — colori fissi non tematizzati (`WIN_COLOR`/`LOSS_COLOR`/`UNKNOWN_COLOR`, coerente col principio "status color" della dataviz skill). La sezione 1 (per singolo avversario, barre andata/ritorno) non ha la striscia, perché lì l'asse X non è temporale. Verificato con screenshot Playwright: quadratini allineati 1:1 sotto le etichette avversario, i 2 playoff correttamente grigi (nessun file ufficiale per loro).

## Workflow settimanale (nuova partita)
1. Aggiungi riga in `config/matches.csv`
2. Apri `notebooks/classifiche.ipynb` in Cursor o Colab
3. Esegui cella per cella — il check terne rileva nuovi giocatori
4. Committa `config/players.csv` e `config/player_identities.csv` dopo il check
5. Push su GitHub

## Prossimi sviluppi (vedi docs/roadmap.md)
- Confronto KPI cross-stagione (in standby, richiede più stagioni)
- Eventuale estrazione della sezione "altro" di tabellino.ipynb
- Aggiunta stagione 2026-2027 a settembre
